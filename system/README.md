# System notes

One file per machine the sweep has been run on, recording the values that are
properties of that machine rather than of the benchmark: device mapping, idle
temperatures, DPM range, and the toolchain actually used.

Keep machine-specific numbers here so the top-level `README.md` stays portable.
